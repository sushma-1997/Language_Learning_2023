String message = "Hello, world!";
int length = message.length(); // returns 13
String upper = message.toUpperCase(); // returns "HELLO, WORLD!"
String lower = message.toLowerCase(); // returns "hello, world!"
boolean startsWithHello = message.startsWith("Hello"); // returns true
boolean endsWithWorld = message.endsWith("world!"); // returns true
